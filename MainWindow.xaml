<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="costing_tool.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:costing_tool"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Kongsberg Normanton // Costing Application">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <!-- Outer grid splits window into title bar row and content row -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <!-- Title bar -->
            <RowDefinition Height="*"/>
            <!-- Main content -->
        </Grid.RowDefinitions>

        <!-- Title bar -->
        <Grid x:Name="AppTitleBar" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Title -->
            <StackPanel Grid.Column="0" Orientation="Horizontal"
                        VerticalAlignment="Center" Margin="16,0">
                <TextBlock Text="Kongsberg SAP GUI" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Right: Buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal"
                        VerticalAlignment="Stretch">
                <Button x:Name="MinimiseButton"
                        Content="&#xE921;"
                        Style="{StaticResource MinimizeButtonStyle}"
                        Click="MinimiseButton_Click"/>
                <Button x:Name="CloseButton"
                        Content="&#xE8BB;"
                        Style="{StaticResource CloseButtonStyle}"
                        Click="CloseButton_Click"/>
            </StackPanel>
        </Grid>

        <!-- Main content area -->
        <Frame x:Name="MainFrame" Grid.Row="1"/>

    </Grid>
</Window>